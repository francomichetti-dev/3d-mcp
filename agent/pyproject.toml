[project]
name = "arges-chat"
version = "0.1.0"
description = "Local chat service that drives Fusion 360 via the Claude Agent SDK"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.1.0",
    "aiohttp>=3.9",
    "httpx>=0.27",
]

[tool.uv]
package = false
